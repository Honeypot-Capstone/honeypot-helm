apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "honeypot-helm.fullname" . }}-test-connection"
  labels:
    {{- include "honeypot-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "honeypot-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
